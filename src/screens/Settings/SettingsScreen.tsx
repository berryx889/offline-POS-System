import { Placeholder } from "../Placeholder";

export function SettingsScreen() {
  return (
    <Placeholder title="Settings" phase="Phase 3+">
      Business details and logo, receipt footer, printer selection + test print,
      users & PINs, backups to USB, and recovery-phrase reset.
    </Placeholder>
  );
}
