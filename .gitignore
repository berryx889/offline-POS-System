# dependencies
node_modules/
# build output
dist/
# rust / tauri
src-tauri/target/
src-tauri/gen/
# Tauri bundle icons are tracked because clean CI runners need them to compile.
# Replace them in place with `npm run tauri icon <real logo>` when final branding
# is available.
# env
.env
# os
.DS_Store
Thumbs.db
# local backups produced by the app during dev
backups/
*.ctbk
