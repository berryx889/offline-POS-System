# CounterTop POS is fully offline — there are no cloud secrets.
# This file exists only for local dev toggles.

# Force the dev/browser mock native adapter even inside Tauri (rarely needed).
# VITE_FORCE_MOCK=1
